<div>
	{{name}}

	<button type="button" class="change-devoured btn btn-info" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat{{else}}Repeat{{/if}}
	</button>

	<button type="button" class="delete-burger btn btn-danger" data-id="{{this.id}}">Delete</button>
</div>
